diff --git a/scripts/rebuild-esbuild.js b/scripts/rebuild-esbuild.js
index abcdef1..9999999 100644
--- a/scripts/rebuild-esbuild.js
+++ b/scripts/rebuild-esbuild.js
@@ -1,36 +1,20 @@
-const { execSync } = require('child_process');
-
-function run(cmd) {
-  try {
-    console.log(`> ${cmd}`);
-    execSync(cmd, { stdio: 'inherit' });
-    return true;
-  } catch (err) {
-    return false;
-  }
-}
-
-// Try pnpm rebuild first
-if (run('pnpm rebuild esbuild')) process.exit(0);
-
-// Fallback to npm update-binary rebuild
-console.log('pnpm rebuild failed — trying npm rebuild esbuild --update-binary');
-if (run('npm rebuild esbuild --update-binary')) process.exit(0);
-
-console.error('ERROR: esbuild rebuild failed for both pnpm and npm.');
-process.exit(1);
-
-let serialized;
-let local;
-try {
-  // ...
-} catch (_err) {
-  void _err;
-  console.error(_err);
-}
-
 const { execSync } = require('child_process');

 function run(cmd) {
@@ -39,21 +23,6 @@ function run(cmd) {
   }
 }

-// Try pnpm rebuild first
-if (run('pnpm rebuild esbuild')) process.exit(0);
-
-// Fallback to npm update-binary rebuild
-console.log('pnpm rebuild failed — trying npm rebuild esbuild --update-binary');
-if (run('npm rebuild esbuild --update-binary')) process.exit(0);
-
-console.error('ERROR: esbuild rebuild failed for both pnpm and npm.');
-process.exit(1);
-
-let serialized;
-let local;
-try {
-  // ...
-} catch (_err) {
-  void _err;
-  console.error(_err);
-}
+// Try pnpm rebuild first
+if (run('pnpm rebuild esbuild')) process.exit(0);
+
+// Fallback to npm update-binary rebuild
+console.log('pnpm rebuild failed — trying npm rebuild esbuild --update-binary');
+if (run('npm rebuild esbuild --update-binary')) process.exit(0);
+
+console.error('ERROR: esbuild rebuild failed for both pnpm and npm.');
+process.exit(1);
