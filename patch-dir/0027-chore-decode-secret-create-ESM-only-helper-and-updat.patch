From 57893cf3e163a1ffcce4a905d2101211e4cab1f3 Mon Sep 17 00:00:00 2001
From: Your Name <github-actions[bot]@users.noreply.github.com>
Date: Tue, 25 Nov 2025 12:02:24 +0800
Subject: [PATCH 27/34] chore(decode-secret): create ESM-only helper and update
 test

---
 supabase/scripts/decode-secret.js | 16 +++++-----------
 1 file changed, 5 insertions(+), 11 deletions(-)

diff --git a/supabase/scripts/decode-secret.js b/supabase/scripts/decode-secret.js
index b6173c16..c3b2756b 100644
--- a/supabase/scripts/decode-secret.js
+++ b/supabase/scripts/decode-secret.js
@@ -1,17 +1,13 @@
-// supabase/scripts/decode-secret.js
-// Small helpers to safely decode Base64-encoded secrets from environment variables.
+/* supabase/scripts/decode-secret.js
+   ESM-only helpers: decodeEnvBase64, tryDecodeEnvBase64
+*/
 
 function isLikelyBase64(s) {
   if (typeof s !== 'string' || s.length === 0) return false;
   const trimmed = s.trim();
-
-  // Only valid base64 chars + optional padding
   if (!/^[A-Za-z0-9+/=]+$/.test(trimmed)) return false;
-
-  // Normalize padding to validate correctly
-  const pad = trimmed.length % 4;
+  const pad = trimmed.length - Math.floor(trimmed.length / 4) * 4;
   const normalized = pad === 0 ? trimmed : trimmed + '='.repeat(4 - pad);
-
   try {
     const buf = Buffer.from(normalized, 'base64');
     const re = buf.toString('base64').replace(/=+$/, '');
@@ -26,11 +22,10 @@ function decodeEnvBase64(name) {
   const raw = process.env[name];
   if (!raw) throw new Error(`Missing environment variable: ${name}`);
   if (!isLikelyBase64(raw)) throw new Error(`Environment variable ${name} is not valid base64`);
-
   try {
     return Buffer.from(raw, 'base64').toString('utf8');
   } catch (err) {
-    throw new Error(`Failed to decode ${name}: ${err.message || err}`);
+    throw new Error(`Failed to decode ${name}: ${err && err.message ? err.message : err}`);
   }
 }
 
@@ -38,7 +33,6 @@ function tryDecodeEnvBase64(name) {
   const raw = process.env[name];
   if (!raw) return null;
   if (!isLikelyBase64(raw)) return null;
-
   try {
     return Buffer.from(raw, 'base64').toString('utf8');
   } catch {
-- 
2.51.2.windows.1

