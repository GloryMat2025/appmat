From b99bc60a9b457ebd261ebe28c47cbc4ec20e5bd5 Mon Sep 17 00:00:00 2001
From: Your Name <github-actions[bot]@users.noreply.github.com>
Date: Tue, 25 Nov 2025 11:36:24 +0800
Subject: [PATCH 24/34] chore(decode-secret): convert to ESM-only module and
 update test

---
 supabase/scripts/decode-secret.js | 25 +++++++++----------------
 supabase/scripts/test-decode.js   |  6 ++++++
 2 files changed, 15 insertions(+), 16 deletions(-)
 create mode 100644 supabase/scripts/test-decode.js

diff --git a/supabase/scripts/decode-secret.js b/supabase/scripts/decode-secret.js
index 079c2476..67a5be49 100644
--- a/supabase/scripts/decode-secret.js
+++ b/supabase/scripts/decode-secret.js
@@ -1,24 +1,19 @@
 // supabase/scripts/decode-secret.js
 // Small helpers to safely decode Base64-encoded secrets from environment variables.
-// Exported helpers:
-//  - decodeEnvBase64(name): returns decoded string or throws on missing/malformed value
-//  - tryDecodeEnvBase64(name): returns decoded string or null on missing/malformed
 
 function isLikelyBase64(s) {
   if (typeof s !== 'string' || s.length === 0) return false;
   const trimmed = s.trim();
 
-  // Only valid base64 chars plus optional padding (=)
+  // Only valid base64 chars + optional padding
   if (!/^[A-Za-z0-9+/=]+$/.test(trimmed)) return false;
 
-  // Length must be multiple of 4 once padded; add padding if needed for validation
+  // Normalize padding to validate correctly
   const pad = trimmed.length % 4;
   const normalized = pad === 0 ? trimmed : trimmed + '='.repeat(4 - pad);
 
-  // Try decode/encode roundtrip to verify it's real base64
   try {
     const buf = Buffer.from(normalized, 'base64');
-    // Re-encode and compare ignoring trailing '=' padding
     const re = buf.toString('base64').replace(/=+$/, '');
     const orig = trimmed.replace(/=+$/, '');
     return re === orig;
@@ -31,11 +26,11 @@ function decodeEnvBase64(name) {
   const raw = process.env[name];
   if (!raw) throw new Error(`Missing environment variable: ${name}`);
   if (!isLikelyBase64(raw)) throw new Error(`Environment variable ${name} is not valid base64`);
+
   try {
-    const buf = Buffer.from(raw, 'base64');
-    return buf.toString('utf8');
+    return Buffer.from(raw, 'base64').toString('utf8');
   } catch (err) {
-    throw new Error(`Failed to decode ${name} as base64: ${err.message || err}`);
+    throw new Error(`Failed to decode ${name}: ${err.message || err}`);
   }
 }
 
@@ -43,17 +38,15 @@ function tryDecodeEnvBase64(name) {
   const raw = process.env[name];
   if (!raw) return null;
   if (!isLikelyBase64(raw)) return null;
+
   try {
-    const buf = Buffer.from(raw, 'base64');
-    return buf.toString('utf8');
+    return Buffer.from(raw, 'base64').toString('utf8');
   } catch {
     return null;
   }
 }
-const { decodeEnvBase64 } = require('./supabase/scripts/decode-secret');
-const SERVICE_ROLE_KEY = decodeEnvBase64('SUPABASE_SERVICE_ROLE_KEY_B64');
-module.exports = 
-{
+
+module.exports = {
   decodeEnvBase64,
   tryDecodeEnvBase64,
 };
diff --git a/supabase/scripts/test-decode.js b/supabase/scripts/test-decode.js
new file mode 100644
index 00000000..fd582b20
--- /dev/null
+++ b/supabase/scripts/test-decode.js
@@ -0,0 +1,6 @@
+import { decodeEnvBase64, tryDecodeEnvBase64 } from './scripts/decode-secret.js';
+
+process.env.TEST_B64 = Buffer.from('hello world').toString('base64');
+
+console.log('tryDecodeEnvBase64:', tryDecodeEnvBase64('TEST_B64')); // expect 'hello world'
+console.log('decodeEnvBase64:', decodeEnvBase64('TEST_B64')); // expect 'hello world'
-- 
2.51.2.windows.1

