From 365fa5f9de352bb5dd88eb43cae566bcbe53a83e Mon Sep 17 00:00:00 2001
From: Your Name <github-actions[bot]@users.noreply.github.com>
Date: Tue, 25 Nov 2025 13:53:27 +0800
Subject: [PATCH 34/34] ci(playwright): modernize workflow (no-verify)

---
 .../workflows/playwright-parity-matrix.yml    | 109 +++++-------------
 1 file changed, 32 insertions(+), 77 deletions(-)

diff --git a/.github/workflows/playwright-parity-matrix.yml b/.github/workflows/playwright-parity-matrix.yml
index d2af00ff..14533fef 100644
--- a/.github/workflows/playwright-parity-matrix.yml
+++ b/.github/workflows/playwright-parity-matrix.yml
@@ -2,96 +2,51 @@ name: Playwright Parity Matrix
 
 on:
   push:
-    branches: [main]
-  workflow_dispatch:
-    inputs:
-      run_id:
-        description: 'Manual run id to use in artifact names (optional)'
-        required: false
-        default: ''
+    branches: [ main ]
+  pull_request:
+    branches: [ main ]
 
-env:
-  NODE_VERSION: '18'
-  RUN_ID: ${{ github.event.inputs.run_id || github.run_id }}
+concurrency:
+  group: ${{ github.workflow }}-${{ github.ref }}
+  cancel-in-progress: false
+
+permissions:
+  contents: read
+  checks: write
+  actions: write
 
 jobs:
-  parity:
-    runs-on: ubuntu-latest
+  test:
     strategy:
+      fail-fast: false
       matrix:
+        os: [ubuntu-latest, windows-latest, macos-latest]
         browser: [chromium, firefox, webkit]
+    runs-on: ${{ matrix.os }}
+
     steps:
-      - name: Checkout repository
+      - name: Checkout
         uses: actions/checkout@v4
 
-      - name: Show Node & Git info
-        run: |
-          node --version
-          npm --version || true
-          git --version
-          echo "RUN_ID=${RUN_ID}"
-
-      - name: Cache pnpm store and node_modules
-        uses: actions/cache@v4
+      - name: Setup Node.js
+        uses: actions/setup-node@v4
         with:
-          path: |
-            ~/.pnpm-store
-            node_modules
-            .pnpm-store
-          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}-${{ matrix.browser }}-${{ env.RUN_ID }}
-          restore-keys: |
-            ${{ runner.os }}-pnpm-
-
-      - name: Install pnpm and project deps (robust + verbose)
-        run: |
-          set -e
-          echo "Installing pnpm@8..."
-          npm install -g pnpm@8
-
-          echo "pnpm version: $(pnpm --version)"
-          echo "Using registry: $(pnpm config get registry || npm config get registry)"
-
-          retries=3
-          delay=5
-          i=1
-          until [ $i -gt $retries ]
-          do
-            echo "pnpm install attempt #$i ..."
-            pnpm install --frozen-lockfile --reporter=ndjson && break
-            echo "pnpm install failed, printing diagnostics..."
-            pnpm -v || true
-            pnpm store status || true
-            pnpm config list || true
-            i=$((i+1))
-            echo "Sleeping ${delay}s before retry..."
-            sleep $delay
-            delay=$((delay * 2))
-          done
+          node-version: '18'
 
-          if [ $i -gt $retries ]; then
-            echo "pnpm install failed after $retries attempts, listing workspace for debugging:"
-            ls -la
-            echo "Printing last 200 lines of pnpm logs (if any):"
-            find . -type f -name "pnpm-debug.log" -maxdepth 4 -print -exec tail -n 200 {} \; || true
-            exit 1
-          fi
-        shell: bash
+      - name: Install dependencies
+        run: npm ci
 
-      - name: Ensure Playwright browsers
-        run: |
-          pnpm exec playwright install --with-deps
-        shell: bash
+      - name: Install Playwright browsers
+        run: npx playwright install --with-deps
 
-      - name: Run Playwright exporter (per-browser)
-        run: |
-          mkdir -p artifacts || true
-          echo "Running exporter for ${matrix.browser} with RUN_ID=${RUN_ID}"
-          node --version
-          pnpm exec node scripts/svg-to-png-pw.mjs --browser=${{ matrix.browser }} --out=artifacts/${{ matrix.browser }}-${{ env.RUN_ID }}
-        shell: bash
+      - name: Run Playwright tests (browser: ${{ matrix.browser }})
+        env:
+          PLAYWRIGHT_BROWSERS: ${{ matrix.browser }}
+        run: npx playwright test --project=${{ matrix.browser }} --reporter=list
 
-      - name: Upload parity artifacts
+      - name: Upload Playwright report
+        if: always()
         uses: actions/upload-artifact@v4
         with:
-          name: parity-${{ matrix.browser }}-${{ env.RUN_ID }}
-          path: artifacts/${{ matrix.browser }}-${{ env.RUN_ID }}/*.png
+          name: playwright-report-${{ matrix.os }}-${{ matrix.browser }}
+          path: playwright-report
-- 
2.51.2.windows.1

